.DS_Store
node_modules/
coverage/
dist/

# Runtime state (feature-scoped workspaces, apply lock, active pointer, live
# review transcripts) — generated locally, never shipped with the plugin.
.mavsdd/

# Target repositories used for local E2E sandbox runs.
sample/

# In-repo design documents kept local for now.
docs/
